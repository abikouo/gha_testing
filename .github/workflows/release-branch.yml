---
name: Release

concurrency:
  group: '${{ github.workflow }} @ ${{ github.sha }}'
  cancel-in-progress: true

on:
  issues:
    types:
      - reopened
      - labeled
      - unlabeled
      - opened
      - edited
      - closed

jobs:
  branch:
    permissions:
      contents: write
      pull-requests: write
    uses: abikouo/github_actions/.github/workflows/release-branch.yml@release_v1